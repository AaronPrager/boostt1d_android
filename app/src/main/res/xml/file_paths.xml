<?xml version="1.0" encoding="utf-8"?>
<!-- Private cache directories: the system camera writes a meal photo into one, the Doctor
     Visit PDF is shared from the other. -->
<paths>
    <cache-path name="camera" path="camera/" />
    <cache-path name="pdf" path="pdf/" />
</paths>
