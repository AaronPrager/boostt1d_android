<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose paints everything; this only needs to exist and not flash white on a
         dark-mode launch. -->
    <style name="Theme.BoostT1D" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
